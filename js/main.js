// instantiate a new register
// initialize it with a div that's on the existing HTML page
var register = new Register();
register.initialize($('#r1'));

var register2 = new Register();
register2.initialize($('#r2'));
